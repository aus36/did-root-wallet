// Settings screen for app, blank for now (will include stuff like logout, etc.)

const Settings = () => {
    return (
        <div>
            <h1>Settings</h1>
        </div>
    )
}

export default Settings